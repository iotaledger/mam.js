/**
 * Concatentate a list of arrays.
 * @param arrays The arrays to concatenate.
 * @returns The concatenated arrays.
 * @private
 */
export declare function concatenate(arrays: Int8Array[]): Int8Array;
